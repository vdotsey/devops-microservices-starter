# apiVersion: v1
# kind: Service
# metadata:
#   name: auth
#   namespace: devops-apps
# spec:
#   selector:
#     app: auth
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 3000

apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: devops-apps
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "3000"
spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
